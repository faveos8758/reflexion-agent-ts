# OpenAI (default provider when model is openai/* or unprefixed short name)
OPENAI_API_KEY=sk-...

# Optional: OpenAI-compatible endpoint (e.g. Ollama)
# OLLAMA_BASE_URL=http://localhost:11434

# API server port (default 3000)
PORT=3000
